# Exemplo: cadastro-rural.duckdns.org
DOMAIN=

# `latest` ou o SHA de um commit.
IMAGE_TAG=latest

LOG_LEVEL=info

# openssl rand -base64 24
POSTGRES_PASSWORD=

# openssl rand -base64 32
# Não mudam depois do primeiro `up`. Ver docs/adr/0002.
DOCUMENTO_ENCRYPTION_KEY=
DOCUMENTO_FINGERPRINT_SECRET=

# openssl rand -base64 18
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=
